package controle;

public abstract class Mamifero extends Animal implements Amamentar {
//abstratc não existe de verdade só vai ser extandida 
	//Toda classe  que implementa uma interface tem que dar corpo para os metodos
	//Já a classe abstrata que implementa uma inferface não obrigatoriamente tem que dar corpo para o metodo
	//ela passa essa responsabilidade para dar corpo para o metodo pelas classes que extende ela
}
